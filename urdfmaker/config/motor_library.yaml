# ==============================================================================
# 📚 电机选型数据库
# ==============================================================================

motors:
  # --- 宇树风格大扭矩电机 (用于髋/膝) ---
  Unitree_A1_Joint:
    mass: 0.605       # kg
    radius: 0.05      # m (用于生成 visual cylinder)
    length: 0.06      # m
    effort: 33.5      # Nm (最大扭矩)
    velocity: 21.0    # rad/s (最大转速)
    # 惯量 (可选，如果不填，脚本会根据圆柱体公式自动计算)
    # inertia: {ixx: 0.001, iyy: 0.001, izz: 0.001}

  # --- 宇树风格中型电机 (用于踝/肩) ---
  Unitree_Go1_Joint:
    mass: 0.400
    radius: 0.04
    length: 0.05
    effort: 23.7
    velocity: 30.0

  # --- 小型电机 (用于手腕/脖子) ---
  Small_Actuator:
    mass: 0.150
    radius: 0.025
    length: 0.035
    effort: 5.0
    velocity: 40.0

  # --- 虚拟/无动力关节 (用于被动关节) ---
  Passive_Joint:
    mass: 0.05
    radius: 0.015
    length: 0.02
    effort: 0.0
    velocity: 0.0

# 卓誉 (Zhuoyu) 系列电机
  # ==========================================

  # --- 卓誉 L_低扭矩 (用于手腕/末端/颈部) ---
  # 对应表格: L_低扭矩(7~10) -> 卓誉列
  Zhuoyu_L_10Nm:
    mass: 0.28        # kg (您提供的数据)
    radius: 0.021     # m (外径 42mm / 2)
    length: 0.0415    # m (模组高度 41.5mm)
    effort: 10.0      # Nm (瞬时峰值扭矩)
    velocity: 20.0    # rad/s (表格未标注，暂定默认值，请按需修改)

  # --- 卓誉 ML_中低扭矩 (用于肩部/手肘/踝) ---
  # 对应表格: ML_中低扭矩(15~50) -> 卓誉列
  Zhuoyu_ML_25Nm:
    mass: 0.38        # kg (您提供的数据)
    radius: 0.0275    # m (外径 55mm / 2)
    length: 0.048     # m (模组高度 48mm)
    effort: 25.0      # Nm (瞬时峰值扭矩)
    velocity: 20.0    # rad/s (表格未标注，暂定默认值，请按需修改)

  # --- 卓誉 MH_中高扭矩 (用于髋部/膝部) ---
  # 对应表格: MH_中高扭矩(50~80) -> 卓誉列
  Zhuoyu_MH_55Nm:
    mass: 0.48        # kg (您提供的数据)
    radius: 0.0315    # m (外径 63mm / 2)
    length: 0.0502    # m (模组高度 50.2mm)
    effort: 55.0      # Nm (瞬时峰值扭矩)
    velocity: 20.0    # rad/s (表格未标注，暂定默认值，请按需修改)
